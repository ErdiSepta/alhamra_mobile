import 'package:flutter/material.dart';

class RiwayatTagihanPage extends StatelessWidget {
  const RiwayatTagihanPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Riwayat Tagihan'),
      ),
      body: const Center(
        child: Text('Riwayat Tagihan Page'),
      ),
    );
  }
}
