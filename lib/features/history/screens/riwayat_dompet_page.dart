import 'package:flutter/material.dart';

class RiwayatDompetPage extends StatelessWidget {
  const RiwayatDompetPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Riwayat Dompet'),
      ),
      body: const Center(
        child: Text('Riwayat Dompet Page'),
      ),
    );
  }
}
